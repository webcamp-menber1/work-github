<h5 class="m-5"><span class="bg-light">　ログイン　</span></h5>


<h6 class="m-5"><span class="bg-light">　会員の方はこちらからログイン　</span></h6>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
  　 <label for="email">メールアドレス</label>
    <%= f.email_field :email,id:"email",placeholder:"sample@example.com", autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <label for="password">パスワード</label>
    <%= f.password_field :password,id:"password", autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <!--<%= f.check_box :remember_me %>-->
      <!--<%= f.label :remember_me %>-->
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
<% end %>


<h6 class="m-5"><span class="bg-light">　会員登録がお済みでない方　</span></h6>
<p><%= render "public/shared/links" %>から新規登録を行なってください。</p>
