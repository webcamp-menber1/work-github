<div class="container">
  <div class="row">
    <div class="cake">
      <p class="mx-auto">ようこそ、ながのCAKEへ！<br>
       このサイトは、ケーキ販売のECサイトになります。<br>
       会員でない方も商品の閲覧はできますが、<br>
       購入には会員登録が必要になります。</p>
    </div>
  </div>

  <h3 class="ml-5">新着商品</h3>
  <div class="row">
    <% @items.last(4).each do |item| %>
      <div class="col-2 m-3 mx-auto">
        <div class="row">
          <%= link_to (image_tag item.get_image(200,200)),item_path(item.id) %>
        </div>
        <div class="row mt-2">
          <%= item.name %>
        </div>
        <div class="row mt-2">
          ¥<%= item.price %>
        </div>
      </div>
    <% end %>
  </div>


</div>