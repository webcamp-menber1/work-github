<h5 class="m-5"><span class="bg-light">　注文履歴詳細　</span></h5>

<p>注文情報</p>
<table>
  <tr>
    <th>注文日</th><td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
  </tr>

  <tr>
    <th>配送先</th>
    <td>
      <%= @order.delivery_postal_code %><br>
      <%= @order.delivery_address %><br>
      <%= @order.delivery_name %>
      </td>
  </tr>

  <tr>
    <th>支払方法</th><td><%= Order.payment_methods_i18n[:"#{@order.payment_method}"] %></td>
  </tr>

  <tr>
    <th>ステータス</th><td><%= Order.order_statuses_i18n[:"#{@order.order_status}"] %></td>
  </tr>
</table>

<p>請求情報</p>
<table>
  <tr>
    <th>商品合計</th>
    <td>
      <% @order_details.each do |order_detail| %>
        <% @total += order_detail.price*order_detail.amount %>
      <% end %>
      <%= @total.to_s(:delimited) %>
      </td>
  </tr>

  <tr>
    <th>配送料</th><td><%= @order.postage %></td>
  </tr>

  <tr>
    <th>ご請求額</th><td><%= @order.payment.to_s(:delimited) %></td>
  </tr>
</table>

<p>注文内容</p>
<table>
  <thead>
    <tr>
      <th>商品</th>
      <th>単価（税込）</th>
      <th>個数</th>
      <th>小計</th>
    </tr>
  </thead>

  <tbody>
    <% @order_details.each do |order_detail| %>
      <tr>
        <td><%= order_detail.item.name %></td>
        <td><%= order_detail.price %></td>
        <td><%= order_detail.amount %></td>
        <td><%= order_detail.price*order_detail.amount %></td>
      </tr>
    <% end %>
  </tbody>
</table>



